# Брифинг Alikhan — 20 августа 2026

## Главное
**Вечером после релинка WhatsApp-сессии bridge переехал на порт 3003, а диспетчер читает порт 3000 → приём сообщений снова остановлен (~10 часов).**

- Утром закрыт вчерашний hash-гейт: вместо хрупкого byte-hash — capability-контракт `_bridge_contract_ok()` (проверка моста по поведению endpoints).
- Во второй половине дня сессия ушла в conflict (428/440) → релинк с QR → bridge поднялся на порту **3003**, но диспетчер по-прежнему ждёт **3000**.

## Доказательная цепочка (проверено напрямую)
1. `curl :3003/health` → `{"status":"connected","scriptHash":"32dfb86c3a8a173b","queueLength":0}` — мост жив, но на 3003.
2. `curl :3000/health` → **Connection refused**; `ss -ltnp` слушает только 3003 (порт 3000 свободен).
3. `/tmp/alikhan_commands.log`: с **13:34 UTC** каждый тик `BRIDGE HEALTH ERR ... Connection refused`, последний реальный `GOT N msgs` был в **07:26 UTC**.
4. Артефакты релинка: `QR_scan_this.png` (13:47), `session.bak.20260820_163639_prerelink` (16:36), `session.relink_20260820_171616` (17:16).

## Данные за день
| Таблица | 20.08 | Комментарий |
|---------|-------|-------------|
| `bot_memory_messages` | 2 (до 07:25 UTC) | только утренние — дальше приём упал |
| `bot_memory_facts` | 0 | 7-й день подряд (last = 13.08) |
| `ojr_section3_work_log` | — | max work_date = 16.08 |
| `ojr_photo_log` | 0 | max photo_date = 18.08 |
| `ojr_section1_personnel` | backfill 16.08 (14:33 UTC) | ручная сводка: Алтын-Тас 3 раб.+1 ИТР, Майкадам 15 раб.+1 ИТР, АйБиКон 3 ИТР |

## ⚠️ Что требует решения
1. **Порт 3003 vs 3000 (срочно):** bridge жив на 3003, диспетчер ждёт 3000. Вернуть bridge на 3000 **или** обновить `BRIDGE` в `whatsapp_commands.py` до 3003. Зона Hermes — я не чиню сам (рестарт gateway / смена порта bridge запрещены).
2. **.docx-extractor (от 19.08, не закрыт):** прораб шлёт персонал `.docx`, extractor не парсит текст → `bot_memory_facts` стоят 7-й день. Рекомендация от 19.08 в силе.

## Инфраструктура (ночь 23:05 UTC)
- **Bridge:** connected на :3003, uptime ~5.5ч, scriptHash `32dfb86c3a8a173b`. На :3000 — никто не слушает.
- **document-extractor:** :8099 ok=true. **Gateway:** active (PID 1631859).
- **git:** 1 коммит за сутки (`702c074` auto-sync infra 20.08 04:03). Незакоммиченный хвост: `bot/whatsapp_commands.py` (+64), `CHRONOLOGY.md`, `knowledge_graph/graph.json`, `maintenance_report.json`.

## Тренд
Второй день подряд приём падает из-за рассинхрона между bridge и диспетчером: вчера — hash-контракт, сегодня — порт. Урок дня: после **любого** изменения bridge.js (патч, релинк) синхронно сверять не только hash/контракт, но и **порт** диспетчера.
