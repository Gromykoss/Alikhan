# Брифинг Alikhan — 19 августа 2026

## Главное
**Найден вероятный корень 6-дневного молчания текст-сводок: прораб шлёт списки персонала вложением .docx, а document-extractor не извлекает из .docx текст (только metadata).**

- `bot_memory_facts` = **0 уже 6-й день** (`last_fact_created` = 13.08 12:02), `ojr_section1_personnel` и `ojr_section3_work_log` стоят с 15.08.
- Причина не в дисциплине прорабов: **данные приходят**, но теряются на этапе извлечения текста из `.docx`.

## Доказательная цепочка (проверено напрямую)
1. 19.08 14:22 прораб `241455008329920@lid` прислал `19.08.2026.docx` (боевая группа). `extract_ok=true`, но `extracted_text = "[document metadata: …]"` — **текста нет**.
2. `curl :8099/extract-document` на этот файл → `{"ok":true, "text":"[document metadata: …]"}` — extractor не парсит `.docx`.
3. Ручное извлечение (zipfile) показывает реальный **список персонала**: Молдалиев, Ильяс Алманбетов, Шекеев, Арпеков, Баякеев, Таалай Уулу Чубак, Дайырбек Уулу Сыймык (водитель) + транспорт HUNDAI оранжевый 07KG418AEN.
4. Тот же sender шлёт `.docx` с metadata-only extraction с **13.08** — ровно с даты остановки фактов. Для сравнения: `.xlsx` («Ежедневный отчет», 15.08) extractor парсит нормально.

## Данные за день
| Таблица | 19.08 | Комментарий |
|---------|-------|-------------|
| `bot_memory_messages` | 1 image + 1 document | оба из боевой группы |
| `ojr_photo_log` | 0 | фото — «Постороннее фото» (фото официального документа), корректно не логируется |
| `bot_memory_facts` | 0 | 6-й день подряд |
| `ojr_section3_work_log` | 0 | last `work_date` = 15.08 |
| `ojr_section1_personnel` | 0 | last created = 15.08 |

## ⚠️ Рекомендация Сергею
Фикс **document-extractor** — добавить парсинг текста `.docx` (python-docx/zipfile) в `:8099/extract-document` (правка `document_extractor.py`, зона Hermes/Codex Build), **или** договориться с прорабом слать персонал `.xlsx`/текстом. Данные **не потеряны** — файлы на диске, текст извлекается, но в БД не доходит.

## Инфраструктура (ночь 23:00 UTC)
- **Bridge:** connected, `queueLength=29`, `collectQueueLength=0`, `collectJournalSize=0`, uptime ~106ч — стабилен, `collectOnlyChats` = обе группы.
- **document-extractor:** `:8099` ok=true (но `.docx` → metadata-only). **Gateway:** active.
- **git:** 2 коммита за 48ч (`c7699df`, `60f87a0` daily-sync). Незакоммиченный хвост: `CHRONOLOGY.md`, `knowledge_graph/graph.json`, `maintenance_report.json` (KG cron 19.08 20:00 UTC).

## Тренд
Медиа-контур стабилен (фото классифицируются, в т.ч. отсев «посторонних»). Инженерная проблема сместилась с доставки на **извлечение контента**: доставка работает, а парсинг `.docx` — нет. Один точечный фикс extractor должен восстановить поток персонала/объёмов, накопившийся за 6 дней.
