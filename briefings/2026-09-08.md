# Брифинг 2026-09-08

## За 24 часа
- **31 коммит** — активный день: программа **GWT (Golden Workflow Tests) — фазы 1→4.4 закрыты**.
  - Фаза 1 (04:51–05:25): пилот vor-import — `.ci/run_affected.py`, `scenario_map.yaml`, job C workflow; фиксы card-THEN-equalities, watched+=test, dup fail-closed.
  - Фаза 2 (06:06): fail-closed base, per-card override, exact .ci whitelist.
  - Фаза 3 (11:35–11:58): lint карточек (shape, dup ids, strict node pinning).
  - Фаза 4 (13:02–20:27): покрытие доменов сценариями — **qa-parser, avr-generation, ejo-generation** (по 3 сценария + map + paths + fixtures), **data-ingestion** (no-ci smoke, live-DB инвариант); CI-фиксы (psycopg2-binary, no-ci card coverage в checker).
- SDG: открытых интентов **0**, журнал консистентен, все writeback-строки закрыты SHA.
- Knowledge Graph auto-rebuild штатно (built_at 08.09 20:00 UTC).

## Живость (23:00 UTC)
- Bridge :3000 — **connected**, queue 0, uptime ~34.7ч.
- Extractor :8099 — ok.

## Открытые пункты
- E2E канона Б (sandbox-raw-ingest) — не проводился.
- gateway-adapter ACK collect_only_chats (HIGH, вне скоупа) — без изменений.
- bare `python3` в pre-commit hook (бэклог).
- Разбор xlsx от прораба за 05–06.09 (из брифинга 07.09) — статус не подтверждён.
