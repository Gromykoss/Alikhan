# Брифинг Alikhan — 15 августа 2026

## Главное
**День архитектурного усиления + генеральной чистки репо.**

1. **Enforced-модель полномочий построена** — три новых модуля закрывают цепочку «запрос → доказательство → разрешение»:
   - `bot/authority.py` — контракт честности, риск-матрица, fail-closed проверки (без I/O, доказательства передаёт вызывающий код).
   - `bot/claim_gate.py` — fail-closed гейт данных-claims: whitelist таблиц, `SELECT count(*)` как наблюдаемое доказательство; пустая БД → `INCONCLUSIVE`, а не «всё ок».
   - `bot/router.py` — `route()` гейтит резюмирование фактов по `assert_data_claim`.
   - Плюс **production send-deny**: отправка в боевую группу закрыта по умолчанию.
   - Коммиты `bfa9b2f`, `d4353f8`, `75a1bab`.

2. **Генеральная чистка репо** — удалён закоммиченный `bot/venv/` (тысячи файлов), `.bak/.backup` (5 py-бэкапов + 15 ЕЖО-шаблонов ~58 MB), мёртвые артефакты (`wamux`, n8n reminder). Добавлены `ARCHITECTURE_AUDIT.md`, `DATA_CONTRACT.md`, скрипты `daily_parse_gap_check.py` и `bridge_48h_restart.sh`. Коммиты `655d2b3`, `09620a0`, `aeb51c2`.

3. **Документация и граф** — актуализированы `AGENTS.md` (правило возврата в Telegram, секции инфраструктуры), `bot/CONTRACTS.md` (v6), пересобран knowledge graph. Коммит `c55d3b7`.

## Инцидент (утро, до обеда)
**Фото не разбирались в `ojr_photo_log`** — трёхслойный сбой: (1) stale bridge без `/collect-messages` (404 → `[]`), (2) рассинхрон env — песочница выпала из `collectOnlyChats`, (3) флап bridge по `reason 428` (session conflict, 7301 рестартов). Решено: убит stale процесс, выровнен профильный `.env`, добавлен идемпотентный re-drain в `app.get('/collect-messages')` (ПАТЧ 5 в PATCHES.md). Цепочка заработала сквозь контур — 3 тестовых фото + классификация + `photo_ok`. KPI фото-фиксации закрыт.

## Изменения за день (10 коммитов)
| Коммит | Суть |
|--------|------|
| `bfa9b2f` | feat: enforced authority model (claim-gate + production send-deny) |
| `d4353f8` | feat: guard_tool_call — enforced файловая граница профиля |
| `75a1bab` | feat: H4+H5 — claim-gate проверяет counts (0 → INCONCLUSIVE) |
| `655d2b3` | chore: удалить закоммиченный bot/venv/ + .gitignore |
| `09620a0` | chore: удалить .bak/.backup (5 py + 15 ЕЖО) |
| `aeb51c2` | chore: документация/скрипты + удаление мёртвых артефактов |
| `c55d3b7` | docs: актуализация AGENTS/CONTRACTS v6 + knowledge graph |
| `794d26d` | refactor: секреты через secret_config.get_secret() + keepalive БД |
| `8df1623` / `97893d8` | chrono: индексные коммиты хронологии |

## Требует внимания
1. **Незакоммиченный хвост:** `knowledge_graph/graph.json` (+47) и `maintenance_report.json` — пересборка графа ещё не закоммичена.
2. **Падающий тест (предсуществующий):** `test_qa_parser.py::test_grok_hallucination_filter` — `ImportError: save_weather not in db`. Висит со вчера, нужен отдельный разбор.
3. **Утренний инцидент закрыт, но остались 7 утренних фото боевой группы** — их события смыты рестартами до фикса; переслать при необходимости (по договорённости с Сергеем тест отложен до реального входящего).

## Тренд
Насыщенный день: утром — боевой инцидент с доставкой фото (решён с учётом уроков 13.08 — «фото не потеряны, а не разобраны»), днём — большой архитектурный сдвиг в сторону enforced-полномочий (claim-gate, fail-closed, production guard) плюс зачистка репозитория от накопленного балласта. Бот переходит от «реагирует на всё» к «отвечает только на доказанное».
