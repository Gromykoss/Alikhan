# Брифинг Alikhan — 25 августа 2026

## Главное
**Пятый день мёртвого текстового приёма, НО данные в OJR впервые за 5 дней пошли — обходным путём через файлы ЕЖО.** Прорабы шлют ЕЖО-файлы (xlsx/pdf/docx) через WhatsApp → мост качает их в `cache/documents/` → внешний процесс импортирует в `ojr_section3` + `ojr_section1_personnel` с меткой `ejo_v2`. Текстовый интейк (диспетчер, `bot_memory_messages`) по-прежнему ноль. Decrypt-ошибки сегодня прекратились — положительный сдвиг.

- `ojr_section3_work_log`: **+29** за сутки (backfill 21/23/24.08 = 23 + свежие 25.08 = 6).
- `ojr_section1_personnel`: **+18** за сутки (backfill = 14 + свежие 25.08 = 4).
- Импорт шёл двумя пачками: **05:41 UTC** (backfill) и **12:12 UTC** (свежие 25.08).

## Доказательная цепочка (проверено напрямую)
1. `curl :3000/health` → `connected`, uptime ~9.5ч, scriptHash `32dfb86c3a8a173b`, queueLength=0.
2. `systemctl --user show hermes-gateway` → MainPID 135505, active since **13:29:37 UTC** (рестарт сегодня, 3-й день подряд).
3. `ss -tlnp :3000` → node PID 136894 (bridge.js, `--session .../profiles/alikhan/whatsapp/session --mode bot`), стартовал 13:30:52.
4. `tail /tmp/alikhan_commands.log` → `GOT 0 msgs, 211 seen` весь день; `seen.json` mtime = 21.08 07:06 (не двигается).
5. `journalctl -u hermes-gateway` за 25.08 → **0 строк** `inbound message`.
6. `cache/documents/` → файлы за 25.08: xlsx ЕЖО (05:26–10:29), PDF-акты `2-31-1 IBCON` (07:53–08:34), docx-отчёты (06:51, 07:15).
7. БД: `ojr_section3` 29 / `ojr_personnel` 18 за сутки, все `created_by/sync_source='ejo_v2'`; `bot_memory_messages`=0, `bot_memory_facts`=0.
8. `grep "No session found to decrypt" bridge.log` → последняя 24.08 09:27 UTC (за 25.08 новых нет); реконнекты 428/503 продолжаются (последний 503 21:49 UTC).
9. `git log --since="24 hours ago"` → пусто (0 коммитов, последний 20.08).

## Данные за день
| Таблица | 25.08 | Комментарий |
|---------|-------|-------------|
| `ojr_section3_work_log` | **+29** | ✅ backfill 21/23/24 + свежие 25.08, created_by='ejo_v2' |
| `ojr_section1_personnel` | **+18** | ✅ backfill + свежие, sync_source='ejo_v2' |
| `bot_memory_messages` | 0 | 4-й день (max = 21.08) |
| `bot_memory_facts` | 0 | 12-й день (last = 13.08) |
| `ojr_photo_log` | 0 | 7-й день (max = 18.08) |
| `ojr_pass_register` | 32 | без изменений |
| `ojr_weather` | 1 | ✅ max = 25.08 02:00 |

## ⚠️ Что требует внимания
1. **Текстовый приём 4-й день мёртв (срочно, зона Hermes).** Сессия после релинка 20.08 нестабильна. Decrypt-ошибки ушли, но inbound не восстановился — 0 `inbound message`, `seen.json` застрял. Нужен релинк/refresh сессии.
2. **Watchdog не чинит мост (5-й день).** Оба дефекта `watchdog_bridge.py` на месте; восстановление идёт ручным/systemd-рестартом gateway (13:29 UTC сегодня), а не watchdog.
3. **ejo_v2-импорт без атрибуции.** `ojr_section3` идёт с пустым `contractor` и `source_fact_id=NULL` — данные обходят QA-пайплайн и не привязаны к подрядчику. Риск: ЕЖО-отчёт не разложит работы по подрядчикам. Точный источник импорта не установлен (в коде метки `ejo_v2` нет).
4. **Незакоммиченный хвост 5-й день** (~650 строк): `whatsapp_commands.py` (+280/−44), `CHRONOLOGY.md`, `migrate_passes_to_register.py`, `briefings/2026-08-20..25.md`. Пора закоммитить.

## Инфраструктура (ночь 23:00 UTC)
- **Bridge:** connected на :3000, uptime ~9.5ч, scriptHash `32dfb86c3a8a173b`. Под капотом — текстовый приём мёртв, файлы качаются.
- **Gateway:** systemd `hermes-gateway.service`, MainPID 135505, active since 13:29:37. Юниты `-alikhan`/`-gulag` inactive — мост спавнит общий gateway.
- **document-extractor:** :8099 `{ok: true}` (журнал за 25.08 пуст — импорт шёл не через него).
- **git:** 0 коммитов за сутки (последний 20.08). Единственное изменение файлов — авто-пересборка knowledge_graph (20:00).
- **Шум (общий gateway):** LLM 524/503 (Nous inference), OpenRouter payment error, MCP xapi keepalive fail — на WhatsApp не влияет.

## Тренд
Пятый день подряд инфраструктурный рассинхрон по текстовому приёму (21.08 require_mention → 22.08 простой+watchdog → 23.08 decrypt песочница → 24.08 decrypt боевая → 25.08 тишина без decrypt-ошибок). **Позитивный сдвиг 25.08:** decrypt-ошибки прекратились И данные в OJR пошли через файлы ЕЖО (обходной канал работает). Приоритет: (1) релинк/refresh сессии WhatsApp для восстановления текстового приёма, (2) починить watchdog под v6, (3) выяснить источник ejo_v2-импорта и добить атрибуцию contractor, (4) закоммитить 5-дневный хвост.
