# Брифинг 2026-09-06

## За 24 часа
- ~22 коммита, все в зоне **Spec Drift Gate (SDG)** — боевой код `bot/` не тронут.
- SDG bootstrap v1.3.1 (`3c0ef66`): секция в AGENTS.md + журнал `spec_drift_log.md`.
- E2E-циклы v1.4 → v1.4.1 → v1.4.2: DAG-цепочка интентов, D16 abort-hygiene, cleanup probe-файлов. Отрицательные тесты (коммит без интента → FAIL) подтверждены.
- REHAB журнала: 5 исторических хвостов легитимизированы, revert-пары 11:04 оформлены OK-REVERT-PAIR; v1.4.2 (`9676c22`) исправил кривой writeback (SHA указывал на chrono-коммит вместо коммита удаления probe).
- Текущее состояние журнала: открытых интентов 0, журнал консистентен.

## Живость (23:00 UTC)
- Bridge :3000 — **connected**, queue 0, uptime ~11.6ч.
- Extractor :8099 — ok.
- `bot_memory_messages`: **+0 за 24ч** — прорабы не пишут (день недели — воскресенье).
- ЕЖО/ОЖР изменений нет.

## Открытые пункты
- E2E канона Б (sandbox-raw-ingest) — всё ещё ждёт живого сообщения в песочницу.
- gateway-adapter ACK обеих collect_only_chats (HIGH, вне скоупа).
- bare `python3` в pre-commit hook (бэклог, обходится venv-окружением).
