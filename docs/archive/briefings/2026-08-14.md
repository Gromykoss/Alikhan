# Брифинг Alikhan — 14 августа 2026

## Главное
**Фикс: media-сообщения теперь сохраняют `message_time`.** `_insert_media_message` писал фото/документы/видео/empty в `bot_memory_messages` без временной метки — из-за этого дневные сводки (фильтр по `message_time::date` в Asia/Bishkek) их не видели. Добавлен конвертер bridge `timestamp` (unix seconds) → UTC, колонка включена в INSERT, timestamp проброшен во все 4 вызова. Коммит `178a363` (08:30 UTC).

## Статус систем (23:00 UTC)
| Система | Статус | Детали |
|---------|--------|--------|
| Hermes Bridge | ✅ | :3000 connected, queueLength=0, uptime ~11ч, scriptHash `6830e1f5ecbf5470` |
| document-extractor | ✅ | :8099 ok=true |
| Gateway | ✅ | active |
| БД (data-flow) | ✅ | последняя запись `bot_memory_messages` 14.08 16:31 (Asia/Bishkek) — без разрывов |
| Диск | 40% | 76G из 193G |

## Изменения за день
- **`178a363`** — fix: media-записи сохраняют `message_time`. Файлы: `bot/whatsapp_commands.py` (+26), `bot/test_photo_classification.py` (мок → 6-й арг), `CHRONOLOGY.md`.
- **Проверка:** `py_compile` — OK.
- **Не делалось:** рестарт сервисов, изменения схемы БД.

## Требует внимания
1. **Падающий тест (предсуществующий):** `test_qa_parser.py::test_grok_hallucination_filter` — `ImportError: cannot import name 'save_weather' from 'db'`. Не связан с today's fix (18 passed / 1 failed). Нужен отдельный разбор.
2. **Незакоммиченная куча правок висит с 09.08** — удаление venv/.bak/бэкапов шаблонов + ранее накопленные фиксы. Пора закоммитить отдельным коммитом.

## Тренд
Спокойный день после вчерашнего инцидента с доставкой и серии QA-фиксов. Один точечный фикс по связности данных (media → дневные сводки). Очередь bridge разобрана (queueLength=0), данные текут без разрывов. Незакрытый хвост — старый тест-импорт и незакоммиченная очистка репозитория.
