# Брифинг Alikhan — 21 августа 2026

## Главное
**Закрыты оба открытых инцидента: (1) «Алихан не отвечает в песочнице» (2 дня диагноза) и (2) .docx-эскалация от 19.08. Бонусом заведён реестр пропусков.**

- Ответ агента в песочнице восстановлен: `free_response_chats` добавлен в конфиг + разграничение контуров (диспетчер → только боевая, gateway → песочница).
- .docx-извлечение текста теперь локальное (zipfile + ElementTree в `whatsapp_commands.py`), без extractor-сервиса — причина 8-дневной паузы `bot_memory_facts` устранена в коде.
- Документы-пропуска маршрутизируются в новый реестр `ojr_pass_register` (32 записи), а не в Раздел 5 ОЖР.

## Доказательная цепочка (проверено напрямую)
1. `curl :3000/health` → `{"status":"connected","scriptHash":"32dfb86c3a8a173b","queueLength":0}` — bridge вернулся на порт 3000 (вчерашний 3003 закрыт).
2. `config.yaml:31` → `free_response_chats: [120363179621030401@g.us]`, `require_mention: True` — фикс ответа на месте.
3. `ojr_pass_register` = 32 записи (`pass_date` 21.08–21.09), водители с госномерами.
4. `ojr_section1_personnel` = 16 записей `sync_source='ejo_v2'` (созданы 21.08 14:59).

## Данные за день
| Таблица | 21.08 | Комментарий |
|---------|-------|-------------|
| `bot_memory_messages` | 5 (до 13:06 Бишкек) | 4 документа + 1 текст от прораба `241455008329920@lid` |
| `bot_memory_facts` | 0 | 8-й день подряд (last = 13.08) — .docx-фикс в коде, поток ещё не пошёл |
| `ojr_section3_work_log` | backfill до 20.08 | 17.08 (8), 18.08 (8), 20.08 (7) |
| `ojr_section1_personnel` | 16 (ejo_v2) | Майкадам 21 раб.+1 ИТР, АйБиКон 4 ИТР+1 раб., Алтын-Тас 3 раб.+1 ИТР |
| `ojr_pass_register` | 32 | новый реестр пропусков (21.08–21.09) |
| `ojr_photo_log` | 0 | max photo_date = 18.08 |

## ⚠️ Что требует внимания
1. **`bot_memory_facts` — 8 дней 0** (last = 13.08). .docx-фикс устранён в коде, но на момент ночи новые facts ещё не появились. Проверить в ближайший тик: документы от 21.08 13:05 Бишкек должны распарситься в facts/personnel живым потоком (не только через ejo_v2).
2. **Незакоммиченный хвост большой** (~480 строк): `bot/whatsapp_commands.py` (+280/−44), `CHRONOLOGY.md`, `scripts/migrate_passes_to_register.py` (новый), `knowledge_graph/*`. Пора закоммитить.

## Инфраструктура (ночь 23:00 UTC)
- **Bridge:** connected на :3000, uptime ~15.8ч, scriptHash `32dfb86c3a8a173b`, queueLength=0.
- **document-extractor:** :8099 ok=true. **Gateway:** active.
- **git:** 0 коммитов за сутки (последний `702c074` 20.08 04:03). Всё сегодняшнее висит незакоммиченным.

## Тренд
Три дня подряд приём падал из-за рассинхрона bridge↔диспетчер (hash → порт → require_mention). Сегодня оба контура разведены явно, hash-гейт заменён на поведенческий контракт — хрупкая точка устранена. Фокус следующих дней: убедиться, что живой поток facts/personnel восстановился после .docx-фикса.
