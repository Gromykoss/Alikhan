# Брифинг Alikhan — 22 августа 2026

## Главное
**Bridge лежал ~5 часов днём (11:30–16:35 UTC), а watchdog его не восстановил — найден баг v5→v6. Плюс 9-й день без facts и 2-й день незакоммиченный хвост.**

- Приём WhatsApp молчал с ~11:30 до ~16:35 UTC: `watchdog_bridge.py` фиксировал `Connection refused`, но так и не перезапустил мост.
- Причина — **двойной баг watchdog** (см. ниже): счётчик failure сбрасывается каждый тик, а systemd-цель в v6 мертва. Мост восстановлен только ручным рестартом gateway ~16:35.
- Причина самого отключения в 11:30 **не установлена** — в bridge.log видны рекуррентные 428/503 и `No session found to decrypt message` (сессия нестабильна после релинка 20.08).

## Доказательная цепочка (проверено напрямую)
1. `tail /tmp/alikhan_watchdog.log` → с `[2026-08-22T11:30:01Z]` по `[15:05:01Z]` подряд `Health check failed: Connection refused` + `Bridge DOWN (failure #1)` (не растёт выше #1).
2. `curl :3000/health` (сейчас) → `{"status":"connected","queueLength":0,"uptime":23113,"scriptHash":"32dfb86c3a8a173b"}` — мост жив после рестарта gateway (PID 454175, bridge PID 455876 стартовали 16:35/16:37).
3. `read bot/watchdog_bridge.py` → `_failure_count` — глобал процесса, cron гоняет single-shot → счётчик всегда `=1`, порог `FAIL_THRESHOLD=3` недостижим; `_restart_bridge()` шлёт `systemctl --user restart hermes-whatsapp-bridge` (в v6 `inactive`).
4. `git log --since="24 hours ago"` → пусто (последний коммит `702c074` 20.08 04:03).

## Данные за день
| Таблица | 22.08 | Комментарий |
|---------|-------|-------------|
| `bot_memory_messages` | 0 | last = 21.08 13:06 — весь день приём молчал (bridge лежал) |
| `bot_memory_facts` | 0 | 9-й день подряд (last = 13.08) — .docx-фикс в коде, поток не пошёл |
| `ojr_section3_work_log` | 0 | max work_date = 20.08 |
| `ojr_section1_personnel` | 0 новых | max created_at = 21.08 14:59 |
| `ojr_photo_log` | 0 | 4-й день, max photo_date = 18.08 |
| `ojr_weather` | 1 | ✅ max = 22.08 — погода течёт штатно |

## ⚠️ Что требует внимания
1. **Watchdog не работает в v6 (срочно).** Оба дефекта в `bot/watchdog_bridge.py`: (а) счётчик `_failure_count` — в памяти single-shot-процесса, никогда не доходит до порога рестарта; (б) рестарт через `systemctl --user` бьёт в мёртвый v5-юнит (`hermes-whatsapp-bridge` inactive, мост спавнит gateway). Пока это не починить — любой простой моста требует ручного вмешательства Сергея (рестарт gateway). Зона Hermes/Codex.
2. **Сессия WhatsApp нестабильна.** В `bridge.log` рекуррентные `Connection closed (428)`, `stream errored out (503)`, `No session found to decrypt message` для песочницы — риск повторного отвала. Причина отключения 11:30 не выяснена.
3. **`bot_memory_facts` — 9 дней 0.** .docx-фикс задеплоен в коде, но живой поток facts так и не восстановился (последний fact 13.08). Нужно подтвердить, что диспетчер реально парсит входящие .docx в facts/personnel, а не только в ejo_v2.
4. **Незакоммиченный хвост 2-й день** (~480+ строк): `bot/whatsapp_commands.py` (+280/−44), `CHRONOLOGY.md`, `scripts/migrate_passes_to_register.py`, `briefings/*`. Пора закоммитить.

## Инфраструктура (ночь 23:00 UTC)
- **Bridge:** connected на :3000, uptime ~6.4ч, scriptHash `32dfb86c3a8a173b`, queueLength=0. Но под капотом — флаппинг 428/503.
- **document-extractor:** :8099 ok=true. **Gateway:** active (PID 454175).
- **systemd:** `hermes-whatsapp-bridge` inactive (ожидаемо для v6), `alikhan-document-extractor` active.
- **git:** 0 коммитов за сутки.

## Тренд
Четвёртый день подряд приём страдает от инфраструктурных рассинхронов: 19.08 hash-гейт → 20.08 порт 3003/3000 → 21.08 require_mention → 22.08 простой + сломанный watchdog. Общий корень — **мост не самодостаточен**: любой отвал требует ручного рестарта gateway, а автоматика (watchdog) осталась от v5 и бьёт в мёртвую systemd-цель. Фокус: починить watchdog под v6 (рестарт моста через gateway, а не systemctl) — иначе каждый простой = ручная эскалация.
