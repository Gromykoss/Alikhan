# Брифинг Alikhan — 13 августа 2026

## Главное
**Инцидент «сообщения не доходили» локализован и устранён без потерь.** Утром боевая группа перестала попадать в БД (`bot_memory_messages`=0). Корень — конкуренция gateway adapter и диспетчера за `/collect-messages`: события вычитывались gateway без `/collect-ack` и «застревали» в journal. После фикса 15 событий разобраны (10 фото + 2 документа + 2 текста + персонал), ничего не потеряно.

## Статус систем (23:00 UTC)
| Система | Статус | Детали |
|---------|--------|--------|
| Hermes Bridge | ✅ | :3000 connected, queueLength=15, collectJournalSize=0 |
| document-extractor | ✅ | :8099 ok=true |
| Gateway | ✅ | active |
| hermes-whatsapp-bridge | ✅ | active (17h uptime) |
| Диск | 38% | 74G из 193G |

## Изменения за день

### Фиксы QA (6 раундов Diamond, Codex → Grok adversarial, все APPROVED)
- `96498e0` — привязка прораба к подрядчику (`SENDER_TO_CONTRACTOR`: Алексей→майкадам, Максат→айбикон) + фикс бага `len(int)`.
- `4a762f0` — агрегация персонал-фактов по (org, position), fix last-write-wins (10 вместо 23).
- `477e2cc` — first-match классификация итог/специальность, защита от задвоения «итог + разбивка».
- Тесты: 19 passed (test_qa_parser.py).

### Чистка документации
- `fc4bb62` — v5-документация и чужеродный CLAUDE.md → archive/.

## Требует внимания
1. **Незакоммиченная куча правок висит с 09.08** — удаление venv/.bak/бэкапов + фиксы QA. Пора закоммитить.
2. **ojr_incidents пуст** — алерт добавлен 12.08, следить за реальным заполнением.

## Тренд
День активной работы: утренний инцидент с доставкой сообщений закрыт за сутки, три QA-фикса подняли точность парсинга персонала (агрегация и защита от задвоения). Система стабильна, очередь разобрана полностью.
