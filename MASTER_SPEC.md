# MASTER_SPEC — Alikhan (ТЗРК Джеруй)

> Единый мастер-документ. Собран из всех доков и кода проекта. Каждый раздел ссылается на источник. Противоречия — в разделе «Открытые вопросы для Сергея» в конце.
> Дата сборки: 02.09.2026 (ЭТАП 1), актуализировано 03.09.2026 (после ЭТАП 2). Автор: Project-Alikhan (по задаче Hermes).

---

## 1. Бизнес-цель и пользователи

**Что это.** WhatsApp AI-агент для строительной площадки золоторудного месторождения **ТЗРК Джеруй** (Кыргызстан, высота 2700 м). Бот живёт в рабочей WhatsApp-группе, запоминает всё (фото, документы, сообщения) и автоматически формирует строительную отчётность по ГОСТ. *(источник: PROJECT.md:1-5)*

**Кого обслуживает:**
| Роль | Канал | Что делает |
|------|-------|-----------|
| Руководитель (Сергей) | Telegram (управление) | команды, approval, отчёты |
| Прорабы / ИТР / субподрядчики | WhatsApp (боевая) | подают данные: факты, фото, документы |
| Заказчик ОсОО «Альянс-Алтын» | ЕЖО/АВР (продукт) | получает отчётность |
| Генподрядчик ОсОО «АйБиКон» | ОЖР/ЕЖО | ведение журнала |
| Офис (Грок-бот) | webhook-мост | кровля/наружка/сметы (с 31.08.2026) |

*(источники: PROJECT.md:5, CLIENT_GUIDE.md:321-381, COMMUNICATION_CHANNELS.md:10-20, AGENTS.md:11-19)*

**Зачем** (PROJECT.md:7-13): стройка на высоте, прорабы ведут бумажные журналы, руководитель удалённо. Нужен инструмент, который (1) собирает всё из WhatsApp — единственный канал на объекте, (2) превращает сырьё в ГОСТ-отчёты, (3) хранит структурированно для аудита.

**Ключевой компромисс** (PROJECT.md:44-49): WhatsApp вместо своего интерфейса (прорабы не поставят приложение); Hermes Bridge вместо Evolution API (стабильнее); 14 таблиц ОЖР — избыточно, но нужно для аудита; DeepSeek v4 Pro как оркестратор — код только через Codex/Grok Build.

---

## 2. Бизнес-процессы

### Ежедневный цикл (CLIENT_GUIDE.md:224-283, METHODOLOGY.md:1-55)
1. **Погода** — авто 2×/день (07:30 и 16:30 по Бишкеку), запись в `ojr_weather` (Open-Meteo). В группу НЕ шлём. Все даты/время — по Бишкеку (UTC+6).
2. **Сбор** — прорабы пишут/шлют как обычно: факты, фото, документы.
3. **Разбор** — QA-парсер + photo-vision → OJR-таблицы.
4. **Опрос** (по команде) — «запускай опрос» → прорабы отвечают «код = объём».
5. **ЕЖО** — «формируй ЕЖО» → Excel за дату (погода+персонал+объёмы+фото).
6. **АВР** — «формируй АВР» → КС-2/КС-6 за период.

### Форматы сообщений прорабов (METHODOLOGY.md:16-43)
```
Остатки (VOR):  2.1.1 = 85.5 ; 2.4.1 — 120м3
Персонал:       Атантай ИТР 2, рабочих 15
Техника:        Экскаватор 1, самосвал 2, автокран 1
Материалы:      Поставка арматуры 5 тонн
Планы:          План на завтра: бетонирование фундаментов
```

### VOR-коды (docs/data-sources-contracts.md, ALIKHAN_ARCHITECTURE.md:37-40)
- Код ВОР: 3- или 4-частный (`3.3.2`, `7.2.1.1`), разделитель `=`/`—`/`–`/`-`.
- Извлекаются **regex'ом до LLM** (Grok не видит коды → не галлюцинирует).
- Справочник: `report/templates/ВОР.xlsx` + `ВОР_с_расценками.xlsx` — 573 кода ВОР (факт проверен 03.09.2026).

### Команды (CLIENT_GUIDE.md:299-307, METHODOLOGY.md:6-12)
| Команда | Действие |
|---------|----------|
| «раскрой отчет» | шаблон со всеми строками (план месяца) |
| «запускай опрос» | опрос O>0 ∧ U>0 |
| «статус опроса» | сводка собранного |
| «закончить опрос» / «формируй отчет» | закрыть → ЕЖО |
| «формируй ЕЖО» | Excel за дату |
| «формируй АВР» / «кс-2» / «кс-6» | акт работ |
| «снимок дня» | дайджест дня |
| «ежо принудительно» | ЕЖО без проверки полноты |

### Жизненный цикл опроса (из METHODOLOGY.md, консолидировано 05.09.2026)
1. **Старт** — бот читает ЕЖО-шаблон, находит активные коды с остатками > 0.
2. **Опрос** — бот шлёт запрос данных по категориям (VOR / персонал / техника / материалы / планы).
3. **Сбор** — прорабы отвечают, бот парсит: VOR-коды (авто-детект `код = значение`), персонал/техника/материалы/планы через QA-парсер.
4. **Сводка** — после каждого ответа обновлённый статус опроса.
5. **Закрытие** — авто-заполнение пропусков → `fill_ejo.py` → отправка ЕЖО.

### Цикл ЕЖО (из METHODOLOGY.md, консолидировано 05.09.2026)
1. **Авто-заполнение** — `fill_ejo.py` собирает факты из БД + погода + персонал.
2. **Ручная правка** — прораб присылает исправленный .xlsx.
3. **Diff по кодам** — `_update_template_from_correction()` сравнивает по VOR-кодам.
4. **Шаблон** — обновляется из corrected-версии.
5. **Накопительные** — `yesterday_cum()` из предыдущего v1-файла.

### Готовность (% выполнения)
Формула (ALIKHAN_ARCHITECTURE.md / skills alikhan-operations:253-256, 492):
`base = sum(plan × min(fact/plan,1.0)) / sum(plan) × 100` по всем строкам K>0, затем `round(base × 0.94 + 6)` (6% = раздел 1 ПСД, всегда 100%). ⛔ НЕ менять без approval — 26% корректно, «исправление» на 94% было откачено.


---

## 3. ЯДРО СИСТЕМЫ — первый контур: приём → структурирование → хранение ⭐

**Центральное ядро и ПЕРВЫЙ контур — это получение, обработка, структурирование и хранение ВСЕЙ информации (текст, документы, фото, прочее), что попадает в ОБЕ группы (песочница + боевая).** Всё складывается в БД формата ОЖР. Ничего не теряется — каждый пришедший факт фиксируется.

**Отчёты (ЕЖО, АВР, снимок дня) — ВТОРОЙ контур:** они собираются из этой БД, это не самостоятельные процессы, а производные от ОЖР.

### Контракт «снимок любого дня» (по дате d)
Для ЛЮБОЙ даты проект должен вернуть полный структурированный снимок: что происходило на стройке.

### Контракт «снимок дня» (по дате d)
| Срез | Таблица | Содержимое |
|------|---------|-----------|
| Погода | `ojr_weather` (weather_date=d) | t max/min/avg, ветер, осадки, явление |
| Персонал | `ojr_section1_personnel` (активные на d) | ИТР/рабочие по подрядчикам |
| Работы | `ojr_section3_work_log` (work_date=d) | код ВОР, здание, объём, подрядчик |
| Техника | `ojr_section2_equipment` (15-я таблица, ЭТАП 2) | перечень единиц |
| Фото | `ojr_photo_log` (photo_date=d) | путь, здание, AI-описание |
| Документы | `ojr_section5_asbuilt_docs` (doc_date=d) | исполнительная документация |
| Материалы | `ojr_materials` (material_date=d) | поставки |
| Инциденты | `ojr_incidents` (incident_date=d) | происшествия, ТБ |
| Сводка | `ojr_daily_summary` (summary_date=d) | агрегаты, готовность % |
| Сырьё | `bot_memory_messages` (`(message_time|created_at)::date=d`, Бишкек) | ВСЁ исходное из ОБЕИХ групп (текст/фото/док) |

*(источник: DATA_CONTRACT.md:1-28, db/ojr_schema.sql)*

### Ключевое правило разрыва (DATA_CONTRACT.md:20-22)
> «Сырьё (`bot_memory_messages`) растёт, а результат (`ojr_*`) не растёт = **РАЗРЫВ РАЗБОРА, НЕ потеря данных**».

Если за день N новых image в сырье, а `ojr_photo_log` +0 — сломался разбор (код), не «данные пропали». Сырьё цело.

### Граф разбора (DATA_CONTRACT.md:14-18, AGENTS.md data flow)
```
WhatsApp (песочница + боевая) → Bridge :3000 → bot_memory_messages (сырьё)
  ├─ text (сводка) → parse_qa → ojr_section1_personnel / ojr_section3_work_log
  ├─ image (стройка) → _save_prod_photo → ojr_photo_log
  ├─ image (не стройка) → только bot_memory_messages
  ├─ document → _save_prod_document → ojr_section5_asbuilt_docs
  └─ погода (Open-Meteo) → ojr_weather
```

### Отчёты = сборка из ОЖР (второй контур)
- ЕЖО = `fill_ejo.py` → view на `ojr_section3_work_log` + weather + photo + personnel.
- АВР = `avr.py` → КС-2/КС-6. **Целевая модель: из ОЖР** (при синхронизации ОЖР↔ЕЖО). Фактически сейчас `avr.py` читает `ЕЖО_шаблон.xlsx` напрямую — переезд на ОЖР осознанно отложен (критерий 7/7 дней «факт сутки» ещё не достижим без синхронизации). ✅ Сверка 03.09.2026: канон = код (avr.py читает шаблон, реализация переключения отложена).
- Снимок дня = композит из `ojr_photo_log` + `ojr_daily_summary` + сообщений.
- Отчёты НЕ имеют отдельного хранилища фактов; могут писать метаданные/агрегаты в `ojr_daily_summary` (`ejo_file_path`, `ejo_version`, `is_corrected`) + отдельное хранилище отчётов (ЭТАП 2).

### Обратный разбор ЕЖО → ОЖР (решение Сергея 02.09.2026)
Если в группу попадает файл ЕЖО (`ЕЖО_*.xlsx`), он **тоже разбирается на данные** → данные идут в ОЖР (backfill: объёмы, персонал, техника, готовность). ЕЖО — не только output, но и источник данных за пропущенные/скорректированные дни.

### Осознанно вне ojr_* (DATA_CONTRACT.md:24-27)
- image classification ∈ {site_related, unrelated, greeting_card, vision_unavailable}
- image без building-тега
- ЕЖО_*.xlsx — output бота, не исполнительная документация

---

## 4. Архитектура (v6 — прямой Hermes Bridge)

### Схема потока (AGENTS.md:89-98, RUNBOOK.md:26-47)
```
WhatsApp → Hermes Bridge :3000 (Baileys, mode=bot) → Hermes Agent (Alikhan)
           │
           ├── QA-парсер (qa.py) → bot_memory_facts → ojr_section1/3/weather
           ├── ЕЖО (fill_ejo.py) — view на ojr_section3 за дату
           ├── poll.py — опросы персонала
           └── document_extractor.py → :8099
```

**v6 = прямой мост.** Нет `main_waha.py`, `bridge_wrapper.py`, `alikhan.service`, Evolution API — всё историческое (миграция 29.07.2026). Alikhan работает как агент Hermes напрямую. *(AGENTS.md:100-101, INDEX.md:17-20, RUNBOOK.md:55)*

### Сервисы и порты (AGENTS.md:106-146, TECHNICAL_REQUIREMENTS.md)
| Сервис | Порт | Роль |
|--------|------|------|
| `hermes-whatsapp-bridge` | :3000 | Baileys WhatsApp, mode=bot |
| `alikhan-document-extractor` | :8099 | распознавание xlsx/pdf |
| `hermes-gateway` | — | мультиплексор (TG/Discord/Buzz/WA) |
| `evolution-postgres` (Docker) | 5432 | БД, internal only |
| `alikhan.service` | — | ОСТАНОВЛЕН |

⚠️ **Боевая группа** `120363400682390076@g.us` — read-only (только слушает). Песочница `120363179621030401@g.us` — отвечает. *(TECHNICAL_REQUIREMENTS.md:155-159)*


---

## 5. Схема данных (15 таблиц ОЖР + legacy)

### Структура (db/ojr_schema.sql, db/ojr_er_diagram.md)
Корень — `ojr_title_page` (титульный лист, 1 активный). От него FK `title_id` во все разделы.

| Таблица | ГОСТ раздел | Назначение |
|---------|-------------|-----------|
| `ojr_title_page` | титул | заказчик, подрядчик, объект, договор |
| `ojr_section1_personnel` | Р.1 | ИТР-персонал |
| `ojr_section2_design_supervision` | Р.2 | авторский надзор |
| `ojr_section2_visits` | Р.2 | журнал посещений АН |
| `ojr_section3_work_log` | Р.3 | **главная** — выполнение работ |
| `ojr_section4_construction_control` | Р.4 | стройконтроль |
| `ojr_section4_checks` | Р.4 | акты проверок |
| `ojr_section5_asbuilt_docs` | Р.5 | исполнительная документация |
| `ojr_section6_gosstroynadzor` | Р.6 | госстройнадзор |
| `ojr_weather` | — | погода (UNIQUE weather_date) |
| `ojr_photo_log` | — | фотофиксация |
| `ojr_daily_summary` | — | сводка дня (UNIQUE summary_date) |
| `ojr_materials` | — | материалы |
| `ojr_incidents` | — | инциденты/ТБ |

**Канон = 15 таблиц ОЖР (решение Сергея 02.09.2026).** 14 существующих (section1-6 + weather/photo/summary/materials/incidents) + **15-я — учёт техники** (`ojr_section2_equipment`). ✅ Реализовано в ЭТАП 2 (коммит `14bc078`). Разделы 7-14 (комиссионинг, ОТ, экология, качество и т.п.) — НЕ входят в канон, вынесены из скоупа.

> **Примечание (сверка 03.09.2026):** В БД 17 таблиц с префиксом `ojr_*`. 15 канонических + `ojr_pass_register` (legacy реестр пропусков, не входит в канон) + `ojr_vor_reference` (справочник ВОР, добавлен в ЭТАП 2 осознанно). Расхождение учётовое, не баг.

### Legacy таблицы (bot_*)
| Таблица | Роль |
|---------|------|
| `bot_memory_messages` | **сырьё** — весь поток WhatsApp (443+ строк) |
| `bot_memory_facts` | legacy факты (fallback после OJR-миграции) |
| `bot_schedule_phases` | график производства (8 этапов + подзадачи) |
| `bot_poll_state` / `bot_poll_residuals` | опросы |
| `bot_calendar_events` | напоминания |
| `bot_building_profiles` | профили зданий |

*(источник: AGENTS.md:120-125, db/ojr_er_diagram.md:186-215, DATA_CONTRACT.md:3-5)*

### Ключевые UNIQUE-индексы (для ON CONFLICT)
- `uq_ojr_work_log` (work_date, vor_code, building, category)
- `uq_ojr_summary_date` (summary_date)
- `uq_ojr_weather_date` (weather_date)
- personnel slot: (title_id, organization_name, full_name, position, start_date)

*(источник: alikhan-direct-ops skill, db/ojr_schema.sql:196,450,376)*

### Views
`ojr_v_daily_works`, `ojr_v_active_personnel`, `ojr_v_open_gsn_orders`, `ojr_v_open_cchecks`, `ojr_v_recent_weather`. *(ojr_schema.sql:605-677)*

---

## 6. Каналы и модули

### Каналы (COMMUNICATION_CHANNELS.md)
- **WhatsApp** — сбор данных (обязательный, единственный на объекте).
- **Telegram** — управление (рекомендованный второй канал).
- **Discord** — альтернатива второго канала.
- **Buzz agent-bus** — координация агентов (Alikhan = участник).

### Модули bot/ (INDEX.md, RUNBOOK.md:57-74)
| Модуль | Назначение |
|--------|-----------|
| `whatsapp_commands.py` | диспетчер: прямой Bridge poll → команды (v6 точка входа) |
| `qa.py` | QA-парсер: regex VOR + Grok structured JSON |
| `poll.py` | ежедневный опрос |
| `fill_ejo.py` | генератор ЕЖО (view на ojr_section3) |
| `data_sources.py` | контракты данных (12 NamedTuple + функции) |
| `avr.py` | АВР (КС-2/КС-6) |
| `db.py` | PostgreSQL: подключение, запросы, синк facts→OJR (section1/3, weather) |
| `document_extractor.py` | распознавание доков (:8099) |
| `office_forward.py` | пересылка вопросов офисному Грок-боту (31.08.2026) |
| `ejo_backfill.py` | обратный разбор ЕЖО .xlsx → ОЖР (ЭТАП 2) |
| `vor_reference.py` | импорт справочника ВОР в ojr_vor_reference (ЭТАП 2) |
| `db.py` | PostgreSQL подключение |
| `handlers.py` | Grok API (ask_grok_raw) |
| ~~main_waha.py, router.py, bridge_wrapper.py~~ | ИСТОРИЧЕСКИЕ (v6) |

### Данные ЕЖО (колонки листа «Ежедневный отчет», 1-based)
| Кол | Буква | Смысл | Кто пишет |
|-----|-------|-------|-----------|
| 11 | K | контрактный объём | шаблон |
| 12 | L | план сут | fill_ejo |
| 13 | M | факт сут | fill_ejo |
| 14 | N | % сут | fill_ejo |
| 15 | O | план мес | шаблон (фильтр опроса) |
| 16 | P | накоп мес | fill_ejo |
| 17 | Q | % мес | fill_ejo |
| 18 | R | план общ | шаблон |
| 19 | S | накоп общ (с начала СМР) | fill_ejo |
| 20 | T | % общ | fill_ejo |
| 21 | U | остаток | fill_ejo (=O−P) |

*(источник: alikhan-operations:155-170, alikhan-fill-ejo skill)*


---

## 7. Интеграции

| Интеграция | Что делает | Источник |
|-----------|-----------|----------|
| **АйБиКон табель** (Google Sheets/Excel) | ИТР-персонал АйБиКон: PROF_MAP, цвета заливки theme≠0, фильтры | ALIKHAN_ARCHITECTURE.md:12-43 |
| **Open-Meteo** | погода (коорд. 42.284, 72.765) → ojr_weather | AGENTS.md:133 |
| **Grok Vision** (grok-4-latest) | AI-описание фото | handlers.py, PROJECT.md:33 |
| **Grok Build / xAI** | QA-парсинг персонала/инцидентов (structured JSON) | qa.py, PROJECT.md:33 |
| ~~**Ollama**~~ | **УДАЛЁН** (решение Сергея 02.09.2026) | — |
| **STT (faster-whisper)** | голосовые — **в маркетинг/перспективу**, в v6 НЕ реализовано | CLIENT_GUIDE.md:458 |
| **Office-bridge (Грок-бот)** | кровля/наружка/сметы: исходящий POST + входящий office-reply (HMAC). ✅ подключён, Human Gate работает (дефект исправлен директором 02.09.2026) | AGENTS.md:11-19, office_forward.py |
| **Codex / Grok Build CLI** | делегирование кода (Maker/Checker) | AGENTS.md:181-189 |

### Office-мост (31.08.2026, детали)
- Исходящий: `office_forward.py` → POST на `https://api2.cursor.sh/automations/webhook/...` (ключ в `bot/secret_config.json`).
- Входящий: офис → POST `http://72.60.16.105:8644/p/alikhan/webhooks/office-reply` (HMAC V2) → prompt в агентный цикл → Alikhan шлёт финальный текст в чат.
- Латентность офиса: минуты — до ~20 мин (Cursor-очередь). ⚠️ 02.09.2026: закончился лимит, сброс после 04.09.26. НЕ диагноз «мёртв» раньше 5+ мин.

### Google Workspace / Bitrix24 / 1С / SAP (Уровень 3)
**Маркетинг/перспектива** (решение Сергея 02.09.2026) — не реализовано в v6, выносится отдельным направлением развития.

---

## 8. Известные пробелы архитектуры (уроки, не обвинения)

1. **tmpfs-эпоха / фотоархив.** Ранние фото (июль) хранились в `~/.hermes/image_cache/`, директория очищена — 112 файлов из 121 в `ojr_photo_log` ссылаются на несуществующие пути. Живой архив — `~/.hermes/profiles/alikhan/cache/images/` (01.08+). *(факт проверен 02.09.2026)*
2. **Чистка 25.07.2026.** Полный аудит: 49 багов, 2 CRITICAL + 7 HIGH исправлены, 21 MEDIUM + 19 LOW не верифицированы. *(PROJECT.md:41)*
3. **Кэш расходный.** `image_cache`, `document_cache`, `audio_cache` — временные, не источник истины. Сырьё — `bot_memory_messages`.
4. **Обрыв конвейера 21.08.** Текстовый приём и OJR-разбор встали (последние `bot_memory_messages` 21.08 07:06). Прорабы пишут в боевую, диспетчер не видит — см. CHRONOLOGY 30.08, 29.08.
5. **Регресс моста после `hermes update`.** Кастомные правки `bridge.js` (collect-only, failClosed, journal) не закоммичены → `git merge/pull` стирает. *(alikhan-direct-ops pitfalls)*
6. **Stale WebSocket (B23).** Baileys держит TCP при сломанных входящих — health=connected, но сообщения не идут. Лечение — рестарт gateway (только Сергей).
7. **Двойное накопление/дубликаты** процессов main_waha (историческое v5).
8. **Таймаут роутера Buzz** — CLI-вызов профиля alikhan падает по 280s, пока профиль занят активной TG-сессией (29.08.2026).

---

## Решения Сергея (02.09.2026) по открытым вопросам

1. **Таблицы ОЖР → 15** (канон: 14 существующих + 15-я новая — учёт техники `ojr_section2_equipment`; добавить CREATE в ЭТАП 2). Разделы 7-14 — вне скоупа.
2. **Техника → 15-я таблица ОЖР** (не `category='техника'` в work_log).
3. **Хранилище отчётов** — завести отдельное. ЕЖО-файл из группы тоже разбирается → данные в ОЖР (backfill).
4. **АВР → из ОЖР** (целевая модель), при синхронизации ОЖР↔ЕЖО.
5. **Погода → в ОЖР**, время по Бишкеку (UTC+6).
6. **Интеграции Уровня 3 → маркетинг/перспектива** (не бэклог v6).
7. **Ollama → удалён**.
8. **ВОР-справочник → сделано** (ЭТАП 2): таблица `ojr_vor_reference` + `vor_reference.py`, 573 кода (коммит `a2a408c`).
9. **STT → маркетинг/перспектива**.
10. **Офис-мост → подключён, работает** (дефект Human Gate исправлен директором Hermes 02.09.2026).

## Канон данных — закрытые решения (02.09.2026, подтверждены Сергеем)

### 1. Техника → 15-я таблица `ojr_section2_equipment`
- Колонки: `title_id, work_date, equipment_name, equipment_type, quantity, shift, status, operator_name, source_message_id, created_at`.
- Уникальность: `(title_id, work_date, equipment_name)` → ON CONFLICT обновляет `quantity/status`.
- Кто пишет: extractor из сообщений прорабов + обратный разбор ЕЖО (лист «Персонал и техника»).

### 2. ЕЖО → ОЖР (обратный разбор / backfill)
- Лист «Ежедневный отчет»: наименование + колонки → `ojr_section3_work_log` (план сутки L, факт сутки M, план мес O, накоп P, общий план R, накоп S).
- Лист «Персонал и техника» → `ojr_section1_personnel` + `ojr_section2_equipment`.
- Встроенные фото (xl/media) → `ojr_photo_log`.
- Готовность/K853 → секция готовности.
- Дубликаты гасит ON CONFLICT (по work_date + имя работы/ед. техники).

### 3. Синхронизация ОЖР↔ЕЖО (критерий переключения АВР)
- Критерий готовности: 7 дней подряд «факт сутки» по работам в ОЖР == факт в ЕЖО (расхождение 0).
- Пока не сходится 7/7 — АВР читает `ЕЖО_шаблон.xlsx`. Сходится — АВР переключается на ОЖР.
- ⚠️ Текущий статус (03.09.2026): критерий 7/7 не достижим (нет синхронизации), `avr.py` читает шаблон. Реализация переключения осознанно отложена.

---

## Сверка ЭТАПА 2 ↔ MASTER_SPEC.md (03.09.2026)

**Worker A (Codex) → Worker B (Grok Build) adversarial. Вердикт: APPROVED.**

| # | Пункт канона | Статус | Детали |
|---|-------------|--------|--------|
| 1 | 15 таблиц ОЖР | ✅ | БД=17 (15 канонических + pass_register legacy + vor_reference справочник). Расхождение учётовое. |
| 2 | `ojr_section2_equipment` — колонки, UNIQUE, FK, CHECK | ✅ | Коммит `14bc078`. Точное совпадение с каноном. |
| 3 | `ojr_vor_reference` — 573 кода, 554 с ценой | ✅ | Коммит `a2a408c`. |
| 4 | Погода — Бишкек UTC+6, ON CONFLICT weather_date | ✅ | `db.py:62` SET TIME ZONE. `save_weather` ON CONFLICT. |
| 5 | Техника — save_equipment, get_daily_equipment, _canon_equipment_name, EquipmentData | ✅ | Коммит `14bc078`. |
| 6 | Backfill ЕЖО — L/M (col 12/13), готовность по тексту (не 853) | ✅ | Коммит `03088fa`. |
| 7 | ВОР — _code, политика skipped_conflict | ✅ | Коммит `a2a408c`. |
| 8 | АВР/синхронизация 7/7 | ⏸️ | Канон есть, реализация осознанно отложена (синхронизация не достижима). |

**Итог: 8/8 пунктов закрыты (6 реализованы, 1 отложен, 1 учётовое дополнение).**
