# BUGS.md — Алихан (EJO)

> Каждый баг → ID, симптом, причина, fix, тест.

## Активные

| ID | Симптом | Причина | Fix | Тест | Статус |
|----|---------|---------|-----|------|--------|
| AL-004 | Мост в self-chat режиме — сообщения из групп не принимались 3 дня | Миграция с Evolution на Bridge без проверки групп-чатов | `--mode bot` + `WHATSAPP_ALLOWED_USERS="*"` | sandbox: отправить сообщение → бот видит | ✅ 16.07.2026 |
| AL-005 | Ollama таймаут 90с на каждом QA-парсинге | ask_grok() → ask_ollama() для текста без фото | parse_qa() → force_grok=True (минуя Ollama) | QA-парсинг < 5с через Grok | ✅ 16.07.2026 |
| AL-006 | QA-факты персонала без подрядчика (ИТР 1) | is_qa() триггерится на «итр», Grok не требует contractor | Валидация в parse_qa(): skip personnel без имени подрядчика | «ИТР 1» → rejected | ✅ 16.07.2026 |
| AL-007 | Фото потеряны при смене режима моста | self-chat режим дропал все входящие из групп | + mode bot (AL-004) | Отправить фото в песочницу → сохранено в БД | 🔴 не проверено |
| AL-008 | Сообщения теряются при перезапуске бота | Age gate 300с + seed из 326 ID при каждом старте | Сохранять сообщения ДО age gate (уже частично), увеличить окно | Перезапуск бота → сообщения не теряются | 🔴 active |
| AL-009 | Старые факты (5160-5163) без подрядчика в БД | Приняты до фикса AL-006 | Удалить id=5160 (блокировано: данные за 16.07 уже в отчёте) | Факты персонала всегда с contractor | 🔴 ручная очистка |
| AL-010 | Дубликат бота после перезапуска | pkill не убил старый PID автоматически | Ручное убийство зомби | pgrep → только один PID | ✅ 16.07.2026 |
| AL-011 | Бот не логирует в /tmp/bot.log после фонового запуска | Перенаправление вывода потеряно при перезапуске | Явное `> /tmp/bot.log 2>&1` | tail -f показывает live-лог | ✅ 16.07.2026 |

## Исправленные

| ID | Симптом | Причина | Fix | Тест | Дата |
|----|---------|---------|-----|------|------|
| BUG-AL-001 | Дублирование отчётов ЕЖО | Два PID бота гнали fill_ejo.py | Убит старый PID, file lock в fill_ejo.py + guard в close_poll | smoke: только один alikhan.service | 08.07.2026 |
| BUG-AL-002 | Production listener не захватывал фото | Малый лимит страниц (1×50) | 4 страницы × 50, лимит фото 8 строк | smoke: WAHA API жив | 08.07.2026 |
| BUG-AL-003 | Шаблонные данные застывали | Не очищались суточные колонки | Очистка всех суточных колонок перед заполнением | smoke: EJO template на месте | 08.07.2026 |
| AL-013 | В КС-6 терялись названия работ и единицы измерения | Накопительная агрегация сохраняла только код и объём | Метаданные позиции берутся из ВОР и исходных записей | `test_generate_ks6` проверяет название и единицу | 19.07.2026 |
| AL-014 | КС-2/КС-6 включали переданные `work_entries` вне периода | Фильтр дат применялся только к SQL-запросу | Добавлен `_filter_entries()` для входных записей | Тесты генерации с датированными записями | 19.07.2026 |
| AL-015 | Позиции ВОР не находились при различиях формата кода | Сопоставление использовало хрупкое строковое равенство | Коды ВОР нормализуются перед поиском цены и метаданных | `test_generate_ks2` сопоставляет код с прайсом | 19.07.2026 |
| AL-016 | Повторная генерация ЕЖО завышала накопительные P/S | Шаблон уже содержал объём текущего дня, но `v` добавлялся повторно | `template_has_today` запрещает повторное прибавление | Повторная генерация сохраняет прежние P/S | 19.07.2026 |
| AL-017 | Нулевые вчерашние P/S не отличались от отсутствующего ЕЖО | `yesterday_cum()` возвращал нулевой кортеж и при отсутствии файла | При отсутствии отчёта возвращается `None`, реальные нули сохраняются | Чистая генерация корректно выбирает baseline | 19.07.2026 |
| AL-018 | Из сообщения обрабатывался только один код работ | Regex искал одиночную пару `код = объём` | Парсер извлекает несколько пар код/объём | Сообщение с несколькими кодами обновляет каждый код | 19.07.2026 |
| AL-019 | Новая запись опроса получала неверное или пустое здание | Fallback здания не соответствовал общей работе | Fallback исправлен на `building='общая'` | Неизвестный код сохраняется со зданием «общая» | 19.07.2026 |
| AL-020 | Пустые и частично распознанные ответы опроса проходили без обратной связи | Результат парсинга не формировал и не отправлял предупреждения | `poll.py` формирует warnings, `main_waha.py` отправляет их в WhatsApp | Пустой/частичный ответ возвращает предупреждение | 19.07.2026 |
