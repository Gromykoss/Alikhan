# CHRONOLOGY — Хронология изменений Алихан бота

## 08.07.2026 — Fix: дублирование ЕЖО

### Проблема
Два пути вызывали `fill_ejo.py` без проверки на существование отчёта:
- «заполни ежо принудительно» → `fill_ejo.py` → v1
- «закончить опрос» → `close_poll()` → `fill_ejo.py` → v2

Результат: два идентичных файла (v1, v2) с одинаковым timestamp.

### Исправление
Добавлен guard в оба пути (`main_waha.py:450-462` и `main_waha.py:394-406`):
перед вызовом `fill_ejo.py`/`close_poll()` проверяется `glob("/tmp/ЕЖО_{date}_v*.xlsx")`.
Если файлы уже есть → отправляется последний существующий вместо повторной генерации.

### Файлы
- `bot/main_waha.py` (+26 строк, 2 guard-блока)
- Бэкап: `bot/main_waha.py.bak.0708_1127`

## 06.07.2026 — T-128: Восстановлен функционал опроса (poll) остатков работ

### Что сделано
- **Создан модуль `bot/poll.py`**: управление опросом, сбор остатков работ по VOR-кодам
- **DB: новые таблицы** `bot_poll_state` + `bot_poll_residuals` для отслеживания опросов
- **main_waha.py**: интеграция poll.py вместо старого вызова `smart_evening_check.py`
  - `Алихан начать опрос` → создаёт poll в БД, проверяет собранные QA-факты, формирует запрос с остатками работ по активным VOR-кодам
  - Авто-детект ответов прораба: парсинг VOR-кодов (`код = объём`), обновление `bot_poll_residuals`
  - `Алихан статус опроса` / `что собрано` → сводка собранного и недостающего
  - `Алихан закончить опрос` → авто-заполнение недостающих категорий → генерация ЕЖО
  - `Алихан заполни ежо` → проверка активного опроса, если данные не все → подсказка
- **router.py**: расширен `cmd_words` (статус опроса, опрос стоп, и т.д.)
- **Убран прямой вызов** `smart_evening_check.py` — вся логика в poll.py

### Архитектура опроса
1. `start_poll()` — читает активные остатки из шаблона/последнего ЕЖО, сохраняет в `bot_poll_residuals`
2. `build_poll_message()` — формирует сообщение: что собрано (✅), что нет (❌), какие остатки нужны
3. `parse_poll_reply()` — парсит ответ прораба: VOR-коды, персонал, технику, планы
4. `get_poll_status()` — статус опроса, сводка собранного
5. `close_poll()` — авто-заполнение → генерация ЕЖО через `fill_ejo.py`

### Коммиты
- `42fe643` — T-128: poll module full restore (poll.py + main_waha + router)

---

## 02.07.2026 — QA parser fix + авто-шаблон

### Что сделано
- QA parser fix: убран pre-parse в qa.py (дублировал факты)
- Дубликаты в БД удалены
- Авто-шаблон: cron 7adc37a6efc5 ежедневно 8:00 Бишкек (02:00 UTC)
- SIM_DATE = None, бот в реальном времени

---

## 01.07.2026 — График 8 этапов из PDF + контекст восстановления

### Что сделано
- ГРАФИК СМР.pdf → 54 строки (8 этапов + 45 подзадач + авторский надзор)
- Код и ответственный в bot_schedule_phases
- ЕЖО шаблон обновлён из corrected v2

---

## 30.06.2026 — ЕЖО pipeline v2

- Профессии из табеля, diff по кодам, защита от удвоения, data_only
- fill_ejo: все 11 правок оформления
- verify: модуль верификации (Claude Code pattern)

---

## 27.06.2026 — T-114: Голосовые сообщения STT+TTS

- faster-whisper base + Grok пост-коррекция
- edge-tts SvetlanaNeural (бесплатно)
- QA-парсер: разделение вопросов/данных, ИТР/рабочие
- DB fact lookup с фильтром по категории
- Grok-суммаризация фактов
- Погода через Open-Meteo API
- Fuzzy-матчинг имени (Олеган→Алихан)
- Персистентность seen ID
- 3 тестовых скрипта

---

## 26.06.2026 — ЕЖО 26.06 цикл: fill_ejo v4 + опросник

- fill_ejo.py: исправлена индентация, bld_plans, очистка L55
- smart_evening_check.py: разделы 6 (планы по ВОР) и 7 (материалы)
- Опросник с планами/материалами

## 07.07.2026 — 4 бага + Правило №0

- **Исправлены 4 бага:** router.py (45 строк заменено), poll.py (Message.ack), main_waha.py (пробелы), fill_ejo.py (int(None) guard, дата YY)
- **Правило №0:** авто-обновление AGENTS.md и CHRONOLOGY.md после каждого блока работ
- **startup_context.md:** Алихан добавлен в сводку проектов для авто-загрузки контекста
