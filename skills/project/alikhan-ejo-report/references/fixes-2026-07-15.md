# Исправления 2026-07-15 — AL-004 – AL-012

9 критических багов в fill_ejo.py, qa.py, main_waha.py, БД.

## AL-004: QA parser regex
**Баг:** `\s*` матчил только пробелы между префиксом и кодом, не слова.  
**Фикс:** `[\w\s]*?` + fallback is_plan из full_match.

## AL-005: save перед age gate
**Баг:** 5-минутный фильтр ДО save_message → старые сообщения не сохранялись.  
**Фикс:** save_message() ПЕРЕД проверкой возраста.

## AL-006: Планы не блокируются работами
**Баг:** `cd not in dn` блокировал планы для кодов с работами.  
**Фикс:** Планы и работы — отдельно, без взаимной блокировки.

## AL-007: Plan filter по category
**Баг:** Фильтр проверял только текст факта, не category.  
**Фикс:** `cat not in ('объём', 'план', '')` → skip.

## AL-008: Динамические планы
**Баг:** `range(2)` хардкод.  
**Фикс:** `range(len(items))`.

## AL-009: Grok hallucination filter
**Баг:** Grok возвращал коды работ в категории «монтаж» → загрязнение volumes.  
**Фикс:** Фильтр категорий в volumes().

## AL-010: DB cleanup
Удалён [монтаж] 3.1.3 = 105.0 из БД.

## AL-011: Не очищать кумулятивные колонки
**Баг:** Очистка P/S/U перед чтением шаблона → потеря накопленных.  
**Фикс:** Очищать только суточные (12,13,14).

## AL-012: Не добавлять v к шаблонному кумулятиву
**Баг:** `prev_p+v` когда шаблон включает сегодня → двойной счёт.  
**Фикс:** Писать `prev_p` и `prev_s` без v. Шаблон — источник истины.
